name: input
on:
  workflow_dispatch:
    inputs:
     environment:
        description: Choose the eniornment to deploy
        required: true
        default: dev
        type: choice
        options:
          - dev
          - main
          - qa
     artifact_tgs:
        description: Choose the eniornment to deploy
        required: true
        default: 0.0.1
jobs:
  inputparam:
   runs-on: ubuntu-latest
   steps:
     - name: build
       run: echo "this job uses ${{ inputs.artifact_tgs}} executed on ${{ inputs.environment }}"
