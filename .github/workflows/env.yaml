name: env
on: workflow_dispatch
env:
  APP_ENV: "dev"
jobs:
  envjob:
   runs-on: ubuntu-latest
   env:
     APP_ENV: "test"
   steps:
     - name: print env
       run: echo "Welcome to $APP_ENV"
   
